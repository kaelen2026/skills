# CLAUDE.md：Claude Code 入口

本仓库的维护规则以 [AGENTS.md](./AGENTS.md) 为唯一事实源。Claude Code 开始任何修改或评审前，先完整阅读 `AGENTS.md`，并按其中的工作流、skill 结构、硬规则与提交前检查执行。

不要在本文件复制维护细则。需要改规则时，只改 `AGENTS.md`，让本文件保持为入口指针，避免两份指令漂移。
